# 🔒 Security Report

**Date:** 2026-07-04 | **Stack:** Unknown / Unknown

## Summary

Critical security posture with 17 vulnerabilities including dangerous bash command execution enabling RCE when combined with wildcard allowlists. Multiple command injection vectors exist through unsanitized spawn/execSync calls and environment variable manipulation. Wildcard allowlists across all messaging adapters (WhatsApp, iMessage, Telegram, Signal) bypass authorization controls, allowing any user to interact with the system.

## Severity Breakdown

| Severity | Total | Auto-Fixed |
|----------|-------|------------|
| 🔴 critical | 1 | 1 |
| 🟠 high | 1 | 1 |
| 🟡 medium | 11 | 5 |
| 🟢 low | 4 | 2 |

## Findings

### 🔴 Critical

**1. Hardcoded API Key in Configuration** — `server/opencode.json:7`
```
"x-api-key": "ak_IrNotiz279_Is0PjbFMm"
```
A hardcoded API key is exposed in the configuration file. This key provides authentication to the Composio backend service and should never be stored in plain text in configuration files that may be committed to version control. ✅ Auto-fixed

### 🟠 High

**1. Dangerous Tools Enabled - Bash Command Execution** — `clawd/config.js:35`
```
allowedTools: ['Read', 'Write', 'Edit', 'Bash', 'Glob', 'Grep']
```
The 'Bash' tool allows arbitrary command execution. Combined with wildcard allowlists, this creates a remote code execution risk. ✅ Auto-fixed

### 🟡 Medium

**1. Uses execSync which could be vulnerable if user input is passed unsanitized** — `clawd/cli.js`
Uses execSync which could be vulnerable if user input is passed unsanitized ⚠️ Manual review

**2. Uses cors middleware - need to verify CORS configuration is restrictive** — `server/server.js`
Uses cors middleware - need to verify CORS configuration is restrictive ⚠️ Manual review

**3. Uses spawn/execFile with external binary path from env var - potential command injection if IMSG_PATH is manipulated** — `clawd/adapters/imessage.js`
Uses spawn/execFile with external binary path from env var - potential command injection if IMSG_PATH is manipulated ⚠️ Manual review

**4. Uses spawn to execute signal-cli - ensure input sanitization** — `clawd/adapters/signal.js`
Uses spawn to execute signal-cli - ensure input sanitization ⚠️ Manual review

**5. Creates HTTP server - verify it's not exposed externally** — `clawd/browser/server.js`
Creates HTTP server - verify it's not exposed externally ⚠️ Manual review

**6. Accesses Chrome profiles which may contain sensitive data** — `clawd/browser/server.js`
Accesses Chrome profiles which may contain sensitive data ⚠️ Manual review

**7. Hardcoded Server URL with HTTP Protocol** — `preload.js:3`
```
const SERVER_URL = 'http://localhost:3001';
```
The server URL is hardcoded and uses insecure HTTP protocol instead of HTTPS. This exposes data in transit to potential interception and man-in-the-middle attacks. ✅ Auto-fixed

**8. Wildcard DM Allowlist - WhatsApp** — `clawd/config.js:5`
```
allowedDMs: ['*'],  // fromMe check handles filtering
```
Wildcard '*' allows messages from any WhatsApp user, relying solely on fromMe check which may be bypassable or misconfigured. ✅ Auto-fixed

**9. Wildcard DM Allowlist - iMessage** — `clawd/config.js:12`
```
allowedDMs: ['*'],           // '*' allows all, or specific chat IDs
```
Wildcard '*' allows messages from any iMessage user without explicit authorization. ✅ Auto-fixed

**10. Wildcard DM Allowlist - Telegram** — `clawd/config.js:19`
```
allowedDMs: ['*'],           // '*' allows all, or specific user IDs
```
Wildcard '*' allows messages from any Telegram user without explicit authorization. ✅ Auto-fixed

**11. Wildcard DM Allowlist - Signal** — `clawd/config.js:26`
```
allowedDMs: ['*'],           // '*' allows all, or specific phone numbers
```
Wildcard '*' allows messages from any Signal user without explicit authorization. ✅ Auto-fixed

### 🟢 Low

**1. Development-only electron-reload enabled based on NODE_ENV** — `main.js`
Development-only electron-reload enabled based on NODE_ENV ⚠️ Manual review

**2. Stores auth state in files - ensure proper file permissions** — `clawd/adapters/whatsapp.js`
Stores auth state in files - ensure proper file permissions ⚠️ Manual review

**3. Hardcoded Empty Telegram Bot Token Placeholder** — `clawd/config.js:20`
```
token: '',                   // Get from @BotFather on Telegram
```
Telegram bot token field should reference an environment variable to prevent accidental hardcoding of secrets. ✅ Auto-fixed

**4. Hardcoded Empty Signal Phone Number Placeholder** — `clawd/config.js:25`
```
phoneNumber: '',             // Your Signal phone number with country code (+1234567890)
```
Signal phone number field should reference an environment variable to prevent accidental hardcoding of PII. ✅ Auto-fixed

## Action Plan

*Generated by Agnixa Recon 2.0 Brain — 2026-07-04T05:42:30.240Z*
